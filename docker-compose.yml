version: "3.9"
services:
  api:
    image: ghcr.io/bangsmackpow/gusto-pos-api:latest
    container_name: gustopos-api
    environment:
      - PORT=3000
      - DATABASE_URL=file:/app/data/gusto.db
    env_file:
      - stack.env
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    # Temporarily disabled read_only to debug connection issues
    read_only: false
    tmpfs:
      - /tmp
      - /run

  frontend:
    image: ghcr.io/bangsmackpow/gusto-pos-frontend:latest
    container_name: gustopos-frontend
    depends_on:
      - api
    ports:
      - "8080:80"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    # Temporarily disabled read_only to debug connection issues
    read_only: false
    tmpfs:
      - /tmp
      - /run
      - /var/cache/nginx
      - /var/log/nginx
      - /var/lib/nginx
